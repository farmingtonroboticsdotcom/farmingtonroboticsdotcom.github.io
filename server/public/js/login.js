// at some point in the future, we should migrate to a google app so that team members can log in with their google accounts directly to access the console

const button = document.getElementById('loading');
